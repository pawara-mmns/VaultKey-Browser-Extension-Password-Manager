{
  "name": "vaultkey-browser",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A local-first password manager that securely stores, generates, and fills credentials directly in your browser.",
  "author": "Pawara Samarawickrama",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite --open popup.html",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prebuild": "npm run generate:icons",
    "build": "npm run typecheck && vite build",
    "generate:icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.2",
    "jsdom": "^30.0.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
