export const STORAGE_KEYS = {
  vaultConfig: "vaultkey.vaultConfig",
  vaultSession: "vaultkey.session",
  generatorSettings: "vaultkey.generatorSettings",
  credentials: "vaultkey.credentials",
} as const;
